# web

Next.js 16 + TypeScript + Tailwind front-end for the trial task. See the [root README](../README.md) for setup, the API contract and the write-up.

```bash
npm install
npm run dev   # http://localhost:3000
```
